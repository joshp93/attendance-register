import { ExportChurchEvent } from './export-church-event';

describe('ExportChurchEvent', () => {
  it('should create an instance', () => {
    expect(new ExportChurchEvent()).toBeTruthy();
  });
});
